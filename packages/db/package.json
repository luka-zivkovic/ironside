{
  "name": "@ironside/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Ironside Postgres baseline and connection pool (owner, projects, machine credentials).",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "import": "./dist/src/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && rm -rf dist/migrations && cp -r migrations dist/migrations",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ironside/shared": "workspace:*",
    "pg": "^8.20.0",
    "ulid": "^3.0.2"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0"
  }
}
